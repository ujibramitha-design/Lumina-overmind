# DOOM Sentinel overlay
# Install this on top of requirements.txt when enabling the full ops / monitoring stack.

# Runtime extras for Sentinel-specific features
python-telegram-bot==20.7
twilio==8.11.0
prometheus-client==0.19.0
sentry-sdk==1.40.6
structlog==23.2.0

# Test and developer tooling
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0

# Optional config / data helpers used by sentinel tooling
python-dotenv==1.0.0
python-dateutil==2.8.2
PyYAML==6.0.1
